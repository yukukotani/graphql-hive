query versions($selector: RegistryVersionsInput!, $limit: Int!, $after: ID) {
  registryVersions(selector: $selector, after: $after, limit: $limit) {
    nodes {
      ...RegistryVersionFields
    }
    pageInfo {
      hasMore
    }
  }
}
